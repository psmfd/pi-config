# agent/extensions/

Pi extensions installed at `~/.pi/agent/extensions/` via `setup.sh`. Each extension is a subdirectory containing an `index.ts` entry point and a `README.md` describing scope, hooks, and overrides. Extensions are loaded by pi at session start and run in the same process as the parent agent.

A subdirectory **without** an `index.ts` is a non-loadable **library** module, not an extension — pi auto-discovers only `*/index.ts`, so libraries ship alongside extensions (for relative `../shared/*.ts` imports) without being loaded. See [`shared/`](shared/README.md) — the Pi Extension Suite foundation (ADR-0030).

## Index

| Extension | Source | Snapshot | Purpose |
|---|---|---|---|
| [`subagent/`](subagent/README.md) | **Vendored** — `@earendil-works/pi-coding-agent@0.80.2` `examples/extensions/subagent/` ([upstream](https://github.com/earendil-works/pi/tree/main/packages/coding-agent/examples/extensions/subagent)) | pi `0.80.2` + the local patches inventoried in [subagent/README.md § Local patches](subagent/README.md#local-patches) (content-hash-tracked by `PATCH_MANIFEST.json`) | Delegates tasks to specialized subagents (single / parallel / chain) in isolated `pi` subprocesses, with ADR-0122 bounded pre-tool runtime provider failover for policy-selected children. Substrate for the orchestrator-protocol routing in `agent/AGENTS.md`. |
| [`secrets-guard/`](secrets-guard/README.md) | **First-party** to this repo | [`psmfd/pi-secrets-guard`](https://github.com/psmfd/pi-secrets-guard) | Blocks `write` / `edit` / `bash` tool calls that would commit or surface secrets (PEM keys, AWS access keys, GitHub PATs, unencrypted vault files, sensitive paths). Runtime layer paired with the `hooks/secrets-guard.sh` git pre-commit hook. |
| [`bash-destructive-guard/`](bash-destructive-guard/README.md) | **First-party** to this repo | [`psmfd/pi-bash-destructive-guard`](https://github.com/psmfd/pi-bash-destructive-guard) | Denies `bash` tool calls invoking destructive operations (`rm`/`mv`, output-redirect clobber, `find -delete`, `dd of=`, `truncate`) against paths outside a configurable safe list; hardened against the GuardFall pipe-sink/glued-substitution bypasses (ADR-0072) and the wrapper-surface/tilde-expansion bypasses (ADR-0112), with an AST second pass (ADR-0100). Consumes [`shared/shell-lex.ts`](shared/README.md). Companion to `secrets-guard`. |
| [`artifact-handoff/`](artifact-handoff/README.md) | **First-party** to this repo | [`psmfd/pi-artifact-handoff`](https://github.com/psmfd/pi-artifact-handoff) | Registers the `artifact_review` tool that writes Tier 3 review-artifact payloads under `.review/` (ADR-0006, ADR-0007). Path-confined to `.review/`; secrets-guard explicitly covers the tool name. |
| [`web-fetch/`](web-fetch/README.md) | **First-party** to this repo | [`psmfd/pi-web-fetch`](https://github.com/psmfd/pi-web-fetch) | Registers the `web_fetch` tool used by research-specialist subagents to corroborate findings against an operator-curated allowlist of first-party documentation hosts (ADR-0015). HTTPS-only, manual redirect re-validation, 256 KB body cap. |
| [`auto-router/`](auto-router/README.md) | **First-party** to this repo | [`psmfd/pi-auto-router`](https://github.com/psmfd/pi-auto-router) | Per-prompt model selection: a cheap classifier on `before_agent_start` picks a credentialed model and applies it via `pi.setModel` (with total fallback). Pi Extension Suite Workstream A; consumes [`shared/`](shared/README.md) (ADR-0031). |
| [`context-manager/`](context-manager/README.md) | **First-party** to this repo | [`psmfd/pi-context-manager`](https://github.com/psmfd/pi-context-manager) | Cache-safe, zero-token context pruning: the `context` hook elides oversized tool output, freezing each decision per `toolCallId` so the cached prefix never churns. Pi Extension Suite Workstream B; consumes [`shared/`](shared/README.md) (ADR-0032). |
| [`indexing/`](indexing/README.md) | **First-party** to this repo | [`psmfd/pi-indexing`](https://github.com/psmfd/pi-indexing) | Semantic `search_codebase` tool over the `cocoindex-code` (`ccc`) CLI engine, with an idle-gated, single-flight `agent_end` background re-index. CLI tool-call path only (no MCP); untrusted-output framing; pinned engine + embedding model. Pi Extension Suite Workstream C (ADR-0033). |
| [`cache-meter/`](cache-meter/README.md) | **First-party** to this repo | [`psmfd/pi-cache-meter`](https://github.com/psmfd/pi-cache-meter) | Read-only `message_end` recorder for the suite-wide prefix-churn / cache-ratio gate: appends per-turn token usage to JSONL when `CACHE_METER_CONFIG` is set (inert otherwise), analyzed offline by `scripts/analyze-cache-ratio.sh`. Never returns a replacement message. Pi Extension Suite cross-cutting (ADR-0034). |
| [`token-meter/`](token-meter/README.md) | **First-party** to this repo | [`psmfd/pi-token-meter`](https://github.com/psmfd/pi-token-meter) | Per-session, per-model token counter: observational `message_end` recorder → per-session JSONL (keyed on pi's session id), with a `token_usage` tool and `scripts/token-meter.sh` CLI. Append-only for parallel-subagent safety; whole-tree accounting; hybrid settings/`--token-meter`/`​/token-meter` toggle, inert by default (ADR-0073). |
| [`gh-identity-guard/`](gh-identity-guard/README.md) | **First-party** to this repo | [`psmfd/pi-gh-identity-guard`](https://github.com/psmfd/pi-gh-identity-guard) | Tool-boundary guard on mutating `gh`/`git push` invocations from the `bash` tool: probes the active gh identity, blocks on drift from the per-repo pin, interactive bootstrap of `.pi/expected-identity` (ADR-0022/0025/0027). |
| [`github-read/`](github-read/README.md) | **First-party** to this repo | n/a | Dynamic `github_read` loader plus typed repository/issue/PR/Actions/Projects and opt-in security/notification readers. Fixed shell-free `gh` argv, identity observation, field projection/redaction, bounded untrusted output; no arbitrary API or mutation surface (ADR-0123, #875). |
| [`git-read/`](git-read/README.md) | **First-party** to this repo | n/a | Mechanically read-only local `git_read` operations for specialists that must not receive `bash`, GitHub tokens, or SSH-agent access; fixed shell-free argv and credential-free child environment (ADR-0123, #881). |
| [`compaction-optimizer/`](compaction-optimizer/README.md) | **First-party** to this repo | [`psmfd/pi-compaction-optimizer`](https://github.com/psmfd/pi-compaction-optimizer) | Compaction *how* + *when*: pre-compaction archive + file-tracker pruning + deterministic/hybrid/llm-only summary modes with a window-relative input gate and output shrink ladder, plus a prefix-cache-aware when-policy (defer mid-phase threshold fires, proactive phase-boundary `ctx.compact()` via `shared/phase-state.ts`); establishes the `extensionSettings.<name>.*` namespace and per-extension data subtree (ADR-0019/0107/0108/0109). |
| [`expertise-client/`](expertise-client/README.md) | **First-party** to this repo | [`psmfd/pi-expertise-client`](https://github.com/psmfd/pi-expertise-client) | Dual-profile client for `agent-expertise-api` (local loopback/API-key **or** upstream bearer/static-OIDC HTTPS): registers read-side `expertise_search` + create-only `expertise_create` (write opt-in, secret-scanned; ADR-0103 superseding ADR-0028, coexistence ADR-0029). Transport/config stack lives in [`shared/`](shared/README.md) `expertise-api-*` since ADR-0095. |
| [`expertise-indexer/`](expertise-indexer/README.md) | **First-party** to this repo | n/a | **Library** (no `index.ts`; not auto-loaded) — the expertise pipeline's pure primitives: canonicalizer (`canonical_blob_sha`), candidate gate, collector (query/render/parse/extract/coalesce), fanout derivation, approval hashing, hardened Form A reader, plus the tsx-invoked `audit-cli.ts` (#595/#598/#608/#601, ADR-0095). |
| [`expertise-fanout-gate/`](expertise-fanout-gate/README.md) | **First-party** to this repo | n/a | Deterministic expertise pipeline (ADR-0095, #613/#605): a `tool_call` hook on `subagent` detects a research-shaped parallel fanout (≥3 tasks, not review-only), runs ONE `expertise_search`, and injects the `CANONICAL_EXPERTISE_RESULTS` block in place (fail-open, session 429 backoff); a `tool_result` hook surfaces returned candidate groups via `ctx.ui.confirm` into a single-use approval ledger; a fail-closed `tool_call` gate on `expertise_create` allows only a recorded full-field approval hash. JSONL telemetry is the #601 audit's first-party artifact. Consumes [`shared/`](shared/README.md) `expertise-api-*` + [`expertise-indexer/`](expertise-indexer/README.md) primitives. |
| [`worktree/`](worktree/README.md) | **First-party** to this repo | n/a | Per-session git worktree isolation with crash-durable WIP snapshots (ADR-0120, #859): the first `write`/`edit` targeting the repo creates a locked `<repo>/.worktrees/<sid>/` branch checkout; primary-checkout writes are denied with a redirect (exemption globs for scratch/handoff files), reads and `bash` are steered into the worktree, subagents inherit its cwd; dirty turn-ends snapshot to `refs/pi-wip/<sid>` (temp-index commits — untracked files included, no hooks, loss bound one turn/timer tick); a session_start reconciler surfaces dead-pid orphans for manual `/worktree resume`. Fail-open with visible warning; `PI_SKIP_WORKTREE=1` to skip. |
| [`payload-tuner/`](payload-tuner/README.md) | **First-party** to this repo | n/a | Per-request wire-payload tuning for local models: `before_provider_request` matches `ctx.model` against user-layer rules and applies non-prefix tweaks only (`chat_template_kwargs` injection, `temperature`/`top_p`, downward `max_tokens` clamp, with per-request defensive vetoes gating kwargs to local completions endpoints and suppressing sampling tweaks under active thinking — ADR-0110). Never touches messages/system/tools (prefix-cache-safe by construction); fail-open; inert by default (ADR-0106, #769). |
| [`package-agent-broker/`](package-agent-broker/README.md) | **First-party** to this repo | n/a | Non-authorizing package-agent review broker (#916, ADR-0128): discovers installed pinned-Git packages' inert `agents/*.json` descriptors via bounded data-only reads, renders a hostile-content-safe immutable snapshot in the TUI, and — after an untimed retype-the-id-and-digest confirmation and a display-to-commit CAS — persists permanently inert review drafts (`activatable: false`, null authorization digest, unresolved provenance enumerated). No model-callable tool, no `registerCommand`; exact raw `pi.on("input")` ingress, interactive-TUI-only, always `handled`. Drafts carry no authority: #917 rejects them unconditionally and owns active grants under a distinct schema/digest domain. Extended by #928/ADR-0129 with the `approve` flow: complete provenance reconstruction (runner content digest, argv policy, and the empty-by-construction extension/module/event closures an isolated child necessarily has), a built-in-only tool policy that refuses `bash`/`write`/`edit`, and a fresh direct-TUI approval that installs a **runtime-scoped active grant** — in-memory only, 4-hour absolute lifetime, 32-identity cap, per-identity authority lock, dying with the process. Drafts and receipts are non-authorizing on every path; approval never dispatches. Completed by #930/ADR-0131 with the dispatch path: a TUI subcommand plus the single static model-callable `package_agent_dispatch` tool (fail-closed, nothing authoritative in the schema) that re-runs full reconstruction under a bounded per-identity authority lock, requires an exact full-tree grant-digest match (`GRANT_POLICY_VERSION` 3), canary-verifies the ADR-0130 OS sandbox through the byte-identical wrapper before every spawn (`.git` masked, `BUN_BE_BUN` probe), spawns from one statically-pinned runner module with the task on stdin and the bwrap payload on an fd pipe, and refuses on any anomaly — bearer-only credentials, no unconfined fallback. Consumes [`shared/`](shared/README.md) `package-agent-review-contract.ts` + `package-agent-grant-contract.ts` + `package-agent-canonical.ts`. |
| [`prefill-meter/`](prefill-meter/README.md) | **First-party** to this repo | n/a | Spawn-time prompt-segment measurement (ADR-0125, #891): on each process's first `before_agent_start`, records per-segment byte counts of the composed system prompt (base / wrapper body / `<project_context>` / `<available_skills>`) plus the task prompt to an append-only JSONL, with a `first_usage` token cross-check from the first `message_end`. Observational only (never modifies the prompt); armed solely by `PREFILL_METER_CONFIG` (inert by default); quantifies the ADR-0124 context-file-suppression delta for #889. |

## Conventions

- Every extension subdirectory MUST contain `index.ts` (the extension entry point) and `README.md` (purpose, hooks, overrides, refusal policy where applicable). A **library** subdirectory (no `index.ts`; e.g. [`shared/`](shared/README.md)) MUST instead contain `README.md` + `tsconfig.json`; `validate.sh` recognizes this form (ADR-0030).
- Vendored extensions MUST record the source pi version in their README, in the relevant commit message when bumped, and (if patched) in a "Local patches" table. See [`subagent/README.md`](subagent/README.md) for the canonical shape — patch description, file, line range, rationale, upstream issue link.
- First-party extensions MUST include a `## Refusal policy (per-rule)` section in the README when they implement pre-flight blocking, classifying each rule as **hard refusal** or **continue-eligible** per the pattern adopted from issue #69. See `secrets-guard/README.md` and `bash-destructive-guard/README.md` for the table shape.
- Override mechanisms MUST be enumerated in the README (`SKIP_*=1` env vars, allowlist files, etc.) with scope and visibility properties.
- Extensions published as overlay mirrors MUST satisfy the [mirror repo requirements](../../docs/outbound-mirror-sync.md#mirror-repo-requirements-drift-prevention): committed lockfile, exact pi-train-aligned `@earendil-works/*` pins, every direct import declared (no transitive-hoisting reliance), and layout-agnostic runtime data-file paths (import specifiers are rewritten by the sync engine; runtime `import.meta.url` paths are not — #736).
- Architectural decisions affecting the extensions layer go in [`adrs/`](../../adrs/) per `agent/rules/adr-required.md`. Current ADRs touching this layer: [ADR-0001](../../adrs/0001-subagent-orchestration-substrate.md) (substrate decision for `subagent/`), [ADR-0005](../../adrs/0005-tool-call-journal-and-restore.md) (Proposed — future `tool-journal/` extension), [ADR-0006](../../adrs/0006-artifact-handoff-and-review-format.md) + [ADR-0007](../../adrs/0007-tier-3-payload-path.md) (substrate for `artifact-handoff/`), [ADR-0008](../../adrs/0008-tier-3-as-sole-intra-session-inter-agent-channel.md) (affirms Tier 3 as the sole intra-session inter-agent channel; supersedes ADR-0002).

## Snapshot-bump workflow (for `subagent/`)

The only vendored extension is `subagent/`. When bumping its snapshot:

1. Copy the new upstream source over `agent/extensions/subagent/index.ts` and `agents.ts` (do NOT touch the local `README.md`).
2. Re-apply the local patches listed in `subagent/README.md` § Local patches. Any patch that has been merged upstream can be dropped from the table. Regenerate `subagent/PATCH_MANIFEST.json` (`scripts/validate-subagent-drift.sh --regenerate`) in the same PR — `validate.sh` fails on a stale manifest.
3. Update the snapshot version in `subagent/README.md` (the opening blockquote near the top of the file, currently of the form `> **Vendored from pi <version>** ...`).
4. Commit with message `chore(extensions): bump subagent snapshot to pi <new-version>` per `agent/rules/conventional-commits.md`.
5. Run `scripts/validate.sh` and `/review` per `agent/rules/post-implementation-review.md` before opening the PR.

This procedure preserves the patch provenance across upstream changes and keeps the merge-conflict surface localized to the documented local patch zone.

## Deferred / future extensions

One extension is designed but not yet implemented; the follow-up issue is gated on ADR promotion:

- **`tool-journal/`** — pre-image snapshot + `restore` tool for write/edit/destructive-bash rollback. Design sketch in [ADR-0005](../../adrs/0005-tool-call-journal-and-restore.md).

### Decided against (do not relitigate)

- **`coms/`** (orchestrator-mediated filesystem journal for subagent-to-subagent evidence exchange) — superseded by [ADR-0008](../../adrs/0008-tier-3-as-sole-intra-session-inter-agent-channel.md), which affirms Tier 3 artifact handoff (`.review/` + `artifact_review` in [`artifact-handoff/`](artifact-handoff/README.md)) as the sole sanctioned intra-session inter-agent channel. Design archive: [ADR-0002](../../adrs/0002-agent-to-agent-channel.md) (Superseded) and [`agent/rules/agent-to-agent-channel.md`](../rules/agent-to-agent-channel.md) (Withdrawn). Re-evaluation trigger is recorded in ADR-0008.

See also [`notes/upstream-deferred.md`](../../notes/upstream-deferred.md) for the full triage record of upstream patterns evaluated and not adopted.
